git init

Create a new GitHub repository:
Go to GitHub and create a new repository. Do not initialize the repository with a README, .gitignore, or license because you'll be doing this locally.

git add .

git commit -m "Initial commit"

git remote add origin your_github_repo_url.git

git push -u origin main
